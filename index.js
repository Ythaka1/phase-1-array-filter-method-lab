// Code your solution here
function findMatching(drivers, names) {
    return drivers.filter(driver => driver.toLowerCase === name.toLowerCase());
    
}
const drivers = [`Bobby`, `Sammy`, `Bobby`, `Sally`]
const name = "Bobby";

console.log (findMatching(drivers, names); {
    
}